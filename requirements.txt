# Eco-Research Copilot 依赖清单
# 安装方式：pip install -r requirements.txt

# 环境变量加载
python-dotenv

# 大模型调用
openai

# 行研框架插件化配置（frameworks/*.yaml）
pyyaml

# Web API 后端（server/）
fastapi
uvicorn
apscheduler

# 搜索工具（可切换 tavily / duckduckgo）
tavily-python
ddgs

# 文档生成
python-docx
requests

# PDF 导出（reportlab 渲染 + markdown 解析）
reportlab
markdown
